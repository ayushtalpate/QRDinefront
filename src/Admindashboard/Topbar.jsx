import React from "react";
import { FaBell, FaSearch, FaMoon, FaSun } from "react-icons/fa";

const Topbar = ({ toggleDarkMode, darkMode }) => {
  return (
    <div className={`d-flex justify-content-between align-items-center p-3 border-bottom ${darkMode ? "bg-dark text-white" : "bg-light"}`}>

     
      </div>
      
  );
};

export default Topbar;
